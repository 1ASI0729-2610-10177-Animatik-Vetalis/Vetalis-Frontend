services:
  - type: web
    name: vetalis-api
    env: node
    buildCommand: npm install -g json-server
    startCommand: json-server db.json --port $PORT --host 0.0.0.0
    envVars:
      - key: PORT
        value: 10000
